# Local development Redis config
# Bind all interfaces inside the container; host publish is limited to 127.0.0.1 in compose.
bind 0.0.0.0
port 6379
protected-mode yes
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru
